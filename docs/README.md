# Project documents

`personal_ai_assistant_master_v0.5.docx` is the current master project document and the primary source of accepted requirements and architectural decisions.

For v0.1 implementation details, see the versioned contracts in the repository root:

- `ACCEPTANCE_V0_1.md`
- `SCHEMA_V0_1.md`
- `IMPLEMENTATION_STATUS.md`

The master document is currently kept in its original DOCX form. A future migration to a text format such as Markdown would make reviews and Git diffs easier without changing its role as the source of truth.
